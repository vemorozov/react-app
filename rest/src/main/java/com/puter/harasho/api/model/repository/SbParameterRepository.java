package com.puter.harasho.api.model.repository;

import com.puter.harasho.api.model.entity.SbParameter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SbParameterRepository extends JpaRepository<SbParameter, Long> {
}
