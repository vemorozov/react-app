package com.puter.rest.repository;

import com.puter.rest.entity.SbParameter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SbParameterRepository extends JpaRepository<SbParameter, Long> {
}
